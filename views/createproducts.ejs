<!DOCTYPE html>
<html lang="en" class="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create New Product</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen">

  <% if (success && success.length > 0) { %>
    <div class="fixed top-4 left-1/2 transform -translate-x-1/2 bg-green-600 text-white text-center py-2 px-4 rounded shadow-lg z-50">
      <%= success[0] %>
    </div>
  <% } %>

  <div class="flex">
    
  
    <aside class="w-64 bg-gray-800 h-screen p-6 shadow-lg">
      <h1 class="text-xl font-semibold mb-8">Baggify</h1>
      <nav class="space-y-4">
        <a href="/shop" class="block text-white hover:font-semibold">Shop</a>
        <a href="/cart" class="block text-white hover:font-semibold">Cart</a>
      </nav>
    </aside>

   
    <main class="flex-1 p-10">
      <h2 class="text-2xl font-bold mb-6">Create New Product</h2>

      <form
        action="/products/create"
        method="POST"
        enctype="multipart/form-data"
        class="bg-gray-800 p-6 rounded shadow-lg"
      >
        
        <div>
          <h3 class="font-semibold text-lg mb-4">Product Details</h3>

          <label class="block mb-2">Product Image</label>
          <input
            type="file"
            name="image"
            class="mb-4 block w-full text-white bg-gray-700 border border-gray-600 rounded p-2"
          />

          <div class="grid grid-cols-2 gap-4">
            <input
              type="text"
              placeholder="Product Name"
              name="name"
              class="border border-gray-600 px-4 py-2 rounded w-full bg-gray-700 text-white"
            />

            <input
              type="text"
              placeholder="Product Price"
              name="price"
              class="border border-gray-600 px-4 py-2 rounded w-full bg-gray-700 text-white"
            />

            <input
              type="text"
              placeholder="Discount Price"
              name="discount"
              class="col-span-2 border border-gray-600 px-4 py-2 rounded w-full bg-gray-700 text-white"
            />
          </div>
        </div>

       
        <div class="mt-6">
          <h3 class="font-semibold text-lg mb-4">Panel Details</h3>

          <div class="grid grid-cols-2 gap-4">
            <input
              type="text"
              placeholder="Panel Color"
              name="panelcolor"
              class="border border-gray-600 px-4 py-2 rounded w-full bg-gray-700 text-white"
            />

            <input
              type="text"
              placeholder="Text Color"
              name="textcolor"
              class="border border-gray-600 px-4 py-2 rounded w-full bg-gray-700 text-white"
            />
          </div>
        </div>

        <button
          type="submit"
          class="mt-6 bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition"
        >
          Create New Product
        </button>
      </form>
    </main>
  </div>
</body>
</html>
